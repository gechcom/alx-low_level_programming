#include "main.h"

/**
 * _strcpy - copy one ponted values to theother
 * @dest: parameter one
 * @src: parameter two
 * Return : the string copied
 */

char *_strcpy(char *dest, char *src)
{
	strcpy(dest, src);
}

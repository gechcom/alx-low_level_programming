#include "main.h"
#include <string.h>
/**
 * _strlen - calcualte the lenth of the string
 * @s: string
 * Return: the lenthe of the string
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}

#include "main.h"
#include <string.h>
/**
 * _strlen - calcualte the lenth of the string
 * @s: string
 * Return: the lenthe of the string
 */

int _strlen(char *s);
{
	int len;

	for (len = 0; *S != '\0'; ++S)
		++S;

	return (len);
}

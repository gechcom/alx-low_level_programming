#include "main.h"

/**
 * is_palindrome - check if a string and its reverse are the same
 * an empty string is a palindrome
 * @s: string 
 * Return: 1 if a string is palindrome and 0 if not
 */

int is_palindrome(char *s)
{
	if (*s == '\0)
		return (1);


}

#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: print the the alphabet
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int numbers = 0;

while (numbers <= 9)
{
	prinf("%i", numbers);
	++numbers;
}
putchar('\n');

return (0);
}
